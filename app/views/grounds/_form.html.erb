<%= form_for(@ground) do |f| %>
  <% if @ground.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ground.errors.count, "error") %> prohibited this ground from being saved:</h2>

      <ul>
      <% @ground.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sport %><br>
    <%= f.text_field :sport %>
  </div>
  <div class="field">
    <%= f.label :open_time %><br>
    <%= f.time_select :open_time %>
  </div>
  <div class="field">
    <%= f.label :close_time %><br>
    <%= f.time_select :close_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
